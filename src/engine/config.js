export const DEFAULT_CONFIG = {
    // Default configuration values for the routing engine
    routes: [],
    maxSpeed: 50, // Maximum speed in km/h
    minSpeed: 5,  // Minimum speed in km/h
    timeout: 300,  // Timeout in seconds
    redirectEnabled: true,
    // Additional configuration options can be added as required
};